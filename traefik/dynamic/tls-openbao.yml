# GENERATED by openbao_traefik_cert.py — do not edit by hand.
# Serves the OpenBao-issued certificate as Traefik's default TLS certificate.
tls:
  stores:
    default:
      defaultCertificate:
        certFile: /etc/traefik/dynamic/openbao-cert.pem
        keyFile: /etc/traefik/dynamic/openbao-key.pem
  certificates:
    - certFile: /etc/traefik/dynamic/openbao-cert.pem
      keyFile: /etc/traefik/dynamic/openbao-key.pem
